using UnityEngine;
using System.Collections;

public class InitHead : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.rotation = Quaternion.Euler(273,24,254);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
